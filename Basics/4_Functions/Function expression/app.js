function sum(x, y) {
  return x + y;
}

/**
 * Function expression (Anonumous function)
 * function is an object and might be stored in a variable
 */
const add = function (x, y) {
  return x + y;
};

const product = function multiply(x, y) {
  return x * y;
};
